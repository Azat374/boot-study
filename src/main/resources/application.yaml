server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5457/studydb
    username: study
    password: studypass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: update
    show-sql: true